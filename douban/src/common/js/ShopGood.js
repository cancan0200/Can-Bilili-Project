export default class ShopGood {
  constructor ({itemId, num, imgUrl, name, desc, price, store}) {
    this.itemId = itemId;
    this.num = num;
    this.imgUrl = imgUrl;
    this.name = name;
    this.desc = desc;
    this.price = price;
    this.store = store;
  }
}
